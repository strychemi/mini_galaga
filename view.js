var view = {
  init: function() {
    var previousTime = 0,
      INTERVAL = 30,
      avatar;

    window.onload = function() {
      var canvas = $("#canvas")[0],
        c = canvas.getContext("2d");
      
    };
  }
};
